<ul class="sections tags">
  {{#each tags as |tag|}}
    <li class="section tag">
      {{yoshinom-tag-link tagObject=tag
                          indexRoute=indexRoute
                          maxCount=tagsMaxCount}}</li>
  {{/each}}
</ul>
